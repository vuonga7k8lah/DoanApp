import { reducerTabNavigator } from './reducerTabNavigator';
import { reducerHeaderNavigator } from './reducerHeaderNavigator';

const reducerAppContent = {
  tabNavigator: reducerTabNavigator,
  headerNavigator: reducerHeaderNavigator,
};

export default reducerAppContent;
