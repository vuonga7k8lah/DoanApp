import { ReactElement } from 'react';
import { RenderHTMLProps } from './shared-types';
interface HTMLLoaderProps extends RenderHTMLProps {
    children: (resolvedHTML: string) => ReactElement;
}
declare function defaultRenderError(props: RenderHTMLProps): JSX.Element;
declare function defaultRenderLoading(): JSX.Element;
declare function LoadHTML(props: HTMLLoaderProps): ReactElement | null;
declare namespace LoadHTML {
    var defaultProps: {
        remoteErrorView: typeof defaultRenderError;
        remoteLoadingView: typeof defaultRenderLoading;
    };
}
export default LoadHTML;
