import { PropsWithChildren } from 'react';
import { PressableProps, StyleProp } from 'react-native';
export interface GenericPressableProps extends Pick<PressableProps, 'onPress' | 'hitSlop'> {
    key?: string | number;
    style?: StyleProp<any>;
    borderless?: boolean;
}
export default function GenericPressable({ key, onPress, style, children, hitSlop, borderless }: PropsWithChildren<GenericPressableProps>): JSX.Element;
