import { ComponentType } from 'react';
import { TNode } from '@native-html/transient-render-engine';
export interface HTMLListElementProps {
    listStyleType: SupportedListStyleType;
    tnode: TNode;
    paddingLeft: number;
    fontSize: number;
    color: string;
    lineHeight: number;
    tagName?: string;
}
interface ListPrefixRendererProps {
    color?: string;
    fontSize: number;
    lineHeight: number;
    index: number;
    nestLevel: number;
}
interface PrefixSepcs {
    Component: ComponentType<ListPrefixRendererProps>;
    computeStrSize(length: number): number;
}
export declare type SupportedListStyleType = 'none' | 'disc' | 'circle' | 'square' | 'decimal' | 'decimal-leading-zero' | 'lower-roman' | 'upper-roman' | 'lower-greek' | 'lower-alpha' | 'lower-latin' | 'upper-alpha' | 'upper-latin' | 'disclosure-open' | 'disclosure-closed';
export interface HTMLListPrefixProps {
    listStyleType?: string;
    defaultListType: string;
}
export default function usePrefixRenderer({ listStyleType, defaultListType }: HTMLListPrefixProps): PrefixSepcs;
export {};
