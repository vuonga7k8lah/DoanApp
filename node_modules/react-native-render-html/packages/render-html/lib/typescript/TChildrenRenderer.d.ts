import { ReactElement, ReactNode } from 'react';
import { TNode } from '@native-html/transient-render-engine';
import { TNodeRendererProps } from './TNodeRenderer';
export interface TChildProps {
    key: string | number;
    childElement: ReactElement;
    index: number;
    childTnode: TNode;
}
export declare type TChildrenRendererProps = {
    disableMarginCollapsing?: boolean;
    tchildren: TNode[];
    renderChild?: (props: TChildProps) => ReactNode;
} & Pick<TNodeRendererProps<TNode>, 'hasAnchorAncestor'>;
declare const TChildrenRenderer: ({ tchildren, hasAnchorAncestor, disableMarginCollapsing, renderChild }: TChildrenRendererProps) => JSX.Element;
export default TChildrenRenderer;
