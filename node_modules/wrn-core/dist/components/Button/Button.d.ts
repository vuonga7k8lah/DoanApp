import { Color, BorderRadius, BorderWidth, Size } from "../../types/types";
import { WithViewStylesProps } from "../../hocs/withViewStyles";
import { TouchableOpacityProps } from 'react-native';
import React, { ReactNode } from 'react';
export interface ButtonProps extends TouchableOpacityProps, WithViewStylesProps {
    children?: ReactNode;
    borderRadius?: BorderRadius;
    size?: Size;
    block?: boolean;
    backgroundColor?: Color;
    color?: Color;
    borderColor?: Color;
    borderWidth?: BorderWidth;
    loading?: boolean;
    loadingColor?: Color;
    LoadingComponent?: ReactNode;
}
declare function ButtonComponent({ children, borderRadius, size, block, activeOpacity, backgroundColor, color, borderWidth, borderColor, loading, loadingColor, LoadingComponent, style, ...otherProps }: ButtonProps): JSX.Element;
export declare const Button: React.MemoExoticComponent<typeof ButtonComponent>;
export {};
