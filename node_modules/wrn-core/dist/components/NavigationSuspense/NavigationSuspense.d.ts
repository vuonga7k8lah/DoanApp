import { PureComponent, ReactNode } from 'react';
interface NavigationSuspenseProps {
    children: ReactNode;
    fallback: ReactNode;
}
interface NavigationSuspenseState {
    isLoaded: boolean;
}
declare type DefaultProps = Pick<NavigationSuspenseProps, 'fallback'>;
export declare class NavigationSuspense extends PureComponent<NavigationSuspenseProps, NavigationSuspenseState> {
    static defaultProps: DefaultProps;
    _req: number;
    constructor(props: NavigationSuspenseProps);
    componentWillUnmount(): void;
    _handleLoaded: () => void;
    render(): ReactNode;
}
export {};
