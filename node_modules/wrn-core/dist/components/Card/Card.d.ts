import { ViewProps } from "../View/View";
import { Color, BorderWidth } from "../../types/types";
import { StyleProp, ViewStyle } from 'react-native';
import React, { ReactNode } from 'react';
export interface CardProps extends ViewProps {
    Header?: ReactNode;
    Body?: ReactNode;
    Footer?: ReactNode;
    style?: StyleProp<ViewStyle>;
    borderColor?: Color;
    borderWidth?: BorderWidth;
    shadow?: boolean;
    backgroundColor?: Color;
    useDivider?: boolean;
    dividerColor?: Color;
    borderRadius?: number;
}
export declare const Card: React.MemoExoticComponent<React.ForwardRefExoticComponent<CardProps & import("../../hocs/withTailwind").WithTailwindProps & React.RefAttributes<unknown>>>;
