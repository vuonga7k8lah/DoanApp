import { InputBaseProps } from "../InputBase/InputBase";
import { BorderWidth, BorderRadius, Color } from "../../types/types";
import { ReactNode } from 'react';
export interface InputProps extends InputBaseProps {
    ClearButtonModeComponent?: ReactNode;
    backgroundColor?: Color;
    borderRadius?: BorderRadius;
    color?: Color;
    borderColor?: Color;
    borderWidth?: BorderWidth;
    onClearText?: InputBaseProps['onChangeText'];
    onFocusText?: InputBaseProps['onChangeText'];
    onFocus?: InputBaseProps['onFocus'];
}
export declare function Input({ onChangeText, onFocusText, onFocus, onClearText, containerStyle, inputStyle, clearButtonMode, ClearButtonModeComponent, placeholder, borderRadius, borderWidth, color, backgroundColor, borderColor, Left, Right, ...otherProps }: InputProps): JSX.Element;
