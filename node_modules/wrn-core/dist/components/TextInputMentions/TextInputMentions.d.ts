import { PureComponent } from 'react';
import { User, TextInputMentionsProps, TextInputMentionsState } from './types';
export declare class TextInputMentions<UserT extends User> extends PureComponent<TextInputMentionsProps<UserT>, TextInputMentionsState<UserT>> {
    static defaultProps: TextInputMentionsProps<any>;
    constructor(props: TextInputMentionsProps<UserT>);
    componentDidUpdate(prevProps: TextInputMentionsProps<UserT>, prevState: TextInputMentionsState<UserT>): void;
    private getCaretPosition;
    private handleDeleteText;
    private handleUpdateRangeOffset;
    private setValueAsync;
    private setTextAddedLengthAsync;
    private handleAddTextInnerRange;
    private handleSearch;
    private handleChangeText;
    private insertNewItemEntityMap;
    private handleUserItemPress;
    private handleSelectionChange;
    private renderInput;
    render(): JSX.Element;
}
