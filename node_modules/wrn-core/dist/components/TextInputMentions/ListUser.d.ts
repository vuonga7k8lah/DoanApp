import { StyleProp, ViewStyle } from 'react-native';
import { User, TextInputMentionsProps } from './types';
export interface ListUserProps<UserT> extends Pick<TextInputMentionsProps<UserT>, 'users' | 'renderUserItem' | 'keyExtractor' | 'hideUserMentioned' | 'keyForMention' | 'readonly' | 'entityMap'> {
    containerStyle?: StyleProp<ViewStyle>;
    search: string;
    onItemPress: (user: UserT) => void;
}
declare function ListUser<UserT extends User>({ users, containerStyle, readonly, renderUserItem, keyExtractor, hideUserMentioned, keyForMention, search, entityMap, onItemPress, }: ListUserProps<UserT>): JSX.Element;
export default ListUser;
