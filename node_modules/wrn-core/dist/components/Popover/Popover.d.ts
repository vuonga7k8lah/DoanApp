import { Placement } from "../../hooks/useSwitchAnimation";
import { Appearance } from "../../types/types";
import { ViewStyle, StyleProp } from 'react-native';
import { ReactNode } from 'react';
export interface PopoverProps {
    appearance?: Appearance;
    placement?: Placement;
    children: ReactNode;
    Content: ReactNode;
    style?: StyleProp<ViewStyle>;
}
export declare function Popover({ children, Content, style, placement, appearance }: PopoverProps): JSX.Element;
