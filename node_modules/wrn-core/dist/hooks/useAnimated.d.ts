import { Animated } from 'react-native';
export declare type AnimatedReturn = [Animated.Value, () => void];
export declare function useAnimated({ toValue, duration, useNativeDriver, }: {
    toValue: number;
    duration?: number;
    useNativeDriver?: boolean;
}): AnimatedReturn;
