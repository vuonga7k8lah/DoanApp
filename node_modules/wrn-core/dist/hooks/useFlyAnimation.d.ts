import { Animated } from 'react-native';
interface Coordinates {
    x: number;
    y: number;
}
export interface FlyAnimation {
    coordinates: Coordinates[];
    duration: number;
}
export declare function useFlyAnimation({ coordinates, duration }: FlyAnimation): {
    animatedX: Animated.AnimatedInterpolation;
    animatedY: Animated.AnimatedInterpolation;
    onFly: () => void;
};
export {};
