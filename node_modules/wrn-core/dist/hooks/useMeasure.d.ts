import { MutableRefObject } from 'react';
import { MeasureOnSuccessCallback } from 'react-native';
export interface Measure {
    x: number;
    y: number;
    width: number;
    height: number;
    pageX: number;
    pageY: number;
}
export interface RefObjectItemType {
    measure: (callback: MeasureOnSuccessCallback) => void;
}
declare type UseMeasureReturnType = [Measure, () => void];
export declare function useMeasure(ref: MutableRefObject<RefObjectItemType | null>): UseMeasureReturnType;
export {};
